import firebase_admin
from firebase_admin import credentials, firestore
import json
import os

# We will write a small node script since we don't have python firebase-admin locally setup easily, but actually we do in the workspace? No, better use Node.js
